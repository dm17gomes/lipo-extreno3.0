Fix two specific issues in my project without changing anything else.

----------------------------------------
ISSUE 1 — VIDEO NOT PLAYING (shows 00:00, black screen on click)
----------------------------------------

The testimonial video loads the thumbnail but does not play in production (works in preview). 
It shows duration 00:00 and stays black when clicked.

Apply these corrections:

1. Move the video file to /public/videos/
2. Make sure the file name has no spaces and is lowercase (example: testimonial.mp4)
3. Replace the video component with this exact structure:

<video
  controls
  playsInline
  preload="auto"
  poster="/images/video-thumbnail.jpg"
  className="w-full h-auto rounded-2xl"
>
  <source src="/videos/testimonial.mp4" type="video/mp4" />
  Your browser does not support the video tag.
</video>

4. Remove any dynamic imports, motion wrappers, or lazy loading around this video.
5. Make sure the file is not imported like:
   import video from ...
   It must load directly from /public.
6. Ensure the file encoding is H.264 + AAC (MP4 standard for Safari compatibility).

Do not modify styling beyond what is written above.

----------------------------------------
ISSUE 2 — OFFERS TOO CLOSE (spacing problem on mobile)
----------------------------------------

In Home.tsx find the pricing grid container.

Replace:

className="grid lg:grid-cols-3 gap-10 max-w-6xl mx-auto mt-16"

With:

className="grid grid-cols-1 lg:grid-cols-3 gap-16 lg:gap-12 max-w-6xl mx-auto mt-16"

Then open OfferCard.tsx and:

1. REMOVE any "flex-grow"
2. Make sure the main container includes:

className="relative rounded-[2.5rem] overflow-hidden border-2 transition-all duration-300 flex flex-col bg-card"

3. Add spacing between content sections by ensuring this wrapper exists:

<div className="p-8 md:p-10 flex flex-col items-center space-y-8">

Do not modify pricing logic or structure.
Only spacing and layout.

----------------------------------------

After changes:
- Rebuild the project
- Confirm no TypeScript errors
- Confirm video plays on mobile Safari
- Confirm spacing between cards is clearly visible on mobile